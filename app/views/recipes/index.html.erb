<p id="notice"><%= notice %></p>

<h1>Recipes</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @recipes.each do |recipe| %>
      <tr>
        <td><%= link_to 'Show', recipe %></td>
        <td><%= link_to 'Edit', edit_recipe_path(recipe) %></td>
        <td><%= link_to 'Destroy', recipe, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<iframe src="https://storefront-ui-7j2tdjoj3-bouqs.vercel.app/flowers/all" width="640" height="480">
    <p>Placeholder text; only shows up if the page DOESNT render!</p>
</iframe>

<br>

<%= link_to 'New Recipe', new_recipe_path %>
